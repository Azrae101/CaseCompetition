"""
ARCHIVED

The Flask app was moved to `archive/app.py` to keep the project root free of
Flask runtime code while the repository is used as a static site.

If you need to run the original Flask app again, use the copy under:

  archive/app.py

This file intentionally exits if executed.
"""
import sys

sys.exit("This file was archived. See archive/app.py for the original Flask app.")
